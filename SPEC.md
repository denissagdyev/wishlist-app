# Social Wishlist
Это мой план реализации тестового задания про «социальный вишлист». Здесь я описываю идею продукта, модель данных, ключевые сценарии и требования к дизайну, чтобы дальше скормить это Cursor и собрать проект.

## Идея
Приложение — это социальный вишлист. Пользователь создаёт списки желаний под разные поводы: день рождения, Новый год, свадьба, переезд и так далее. Внутрь списка он добавляет подарки с названием, ссылкой, ценой и картинкой и делится ссылкой на этот список с друзьями.

По публичной ссылке, без регистрации, друзья могут открыть вишлист, посмотреть подарки, зарезервировать какой‑то подарок, чтобы его никто не дублировал, и скинуться на дорогие подарки частичными вкладами. Для дорогих подарков я хочу показывать прогресс‑бар по сумме сборов.

Владелец вишлиста при этом не должен видеть, кто именно и что именно зарезервировал, и не должен видеть, кто и сколько денег внёс. Для него максимум отображается факт, что по подарку уже есть резерв или что по нему идёт сбор, без имён и цифр.

Все действия гостей (резерв, вклад) должны в реальном времени разъезжаться на все открытые сессии этого вишлиста: если мы сидим на странице вдвоём, и один человек зарезервировал подарок или внёс вклад, второй сразу видит изменения без перезагрузки.

## Стек
Фронтенд я делаю на Next.js с App Router и TypeScript. Для стилей использую Tailwind CSS v4 и shadcn UI (компоненты лежат в `src/components/ui`). Для иконок беру `lucide-react`. Для анимации конфетти в момент, когда сбор по подарку закрыт, можно подключить `canvas-confetti` или похожую библиотеку.

Бэкенд и база — через Supabase. Это PostgreSQL как основная база, Supabase Auth для email+пароля и Supabase Realtime для подписок по WebSocket на изменения в таблицах.

## Модель данных (Supabase)
Для ядра приложения мне нужны четыре таблицы: `wishlists`, `items`, `reservations` и `contributions`.

В таблице `wishlists` я храню списки желаний. Поля такие: `id` как uuid с `gen_random_uuid()` в качестве primary key; `owner_id` как uuid, это id пользователя из `auth.users`; `title` — название списка, обязательное поле; `event_date` — дата события, опциональная; `is_public` как boolean, по умолчанию true, это означает, что по ссылке список доступен без логина; плюс `created_at` и `updated_at` как timestamptz с дефолтом `now()`.

В таблице `items` лежат подарки внутри вишлиста. Здесь мне нужны поля: `id` как uuid primary key, `wishlist_id` как внешний ключ на `wishlists.id` с `on delete cascade`, `name` как текстовое обязательное название подарка, `url` как ссылка на товар (может быть null), `price` как numeric(12,2), тоже nullable; если цена не указана, я воспринимаю этот подарок как обычный без сборов. Дополнительно `image_url` для картинки (nullable) и `created_at` как timestamptz с `now()`.

Резервы я храню в таблице `reservations`. Там есть `id` как uuid PK, `item_id` как внешний ключ на `items.id` с `on delete cascade`, `reserver_name` как текст, в который записываю либо введённое гостем имя, либо имя залогиненного пользователя, и `created_at` как timestamptz с текущим временем.

Скидывания на дорогие подарки идут в таблицу `contributions`. В ней есть `id` uuid PK, `item_id` uuid FK на `items.id` с `on delete cascade`, `contributor_name` как текстовое имя дарителя (гость или пользователь), `amount` как numeric(12,2) с ограничением, что это сумма больше нуля, и `created_at` как timestamptz c `now()`.

## Безопасность (упрощённо для теста)
Чтобы в тестовом задании не вязнуть в настройке RLS и не ловить бесконечные 403, я планирую для этих четырёх таблиц (`wishlists`, `items`, `reservations`, `contributions`) RLS отключить. Ролям `anon` и `authenticated` нужно выдать права select, insert, update и delete на эти таблицы и права usage и select на соответствующие sequences.

Логику прав доступа я закладываю на уровне приложения. Изменять вишлисты и подарки может только их владелец (через `owner_id`). Публичные пользователи по ссылке имеют read‑доступ к публичным вишлистам и могут добавлять записи в `reservations` и `contributions` для конкретного списка.

В текстовом описании итогового решения я отдельно проговорю, что в продакшене надо включать нормальный RLS: ограничивать изменения по `owner_id` и `auth.uid()`, а публичным гостям оставлять только чтение публичных списков и insert в резервы/вклады.

## Основные сценарии
Для аутентификации у меня будут страницы `/login` и `/register`. Там формы с email и паролем, которые работают через Supabase Auth. После успешного логина или регистрации пользователя перенаправляю на `/dashboard`. Если он уже залогинен и открывает `/login` или `/register`, я его сразу отправляю обратно на дашборд.

На `/dashboard` я показываю пользователю его вишлисты. Эта страница доступна только залогиненным. Я поднимаю из базы все записи `wishlists`, где `owner_id` совпадает с id текущего пользователя, и показываю их в виде сетки карточек.

В шапке дашборда слева будет логотип или название приложения, справа — email или аватар пользователя и меню с пунктом «Выйти». Logout реализую через Supabase, после выхода перенаправляю на `/login`.

Карточки вишлистов на дашборде отображаются сеткой. Первая карточка — это большая кнопка «Создать новый список». При клике открывается модалка с формой, в которой нужно ввести название и, по желанию, дату события. На сабмите создаётся запись в `wishlists` с `owner_id` текущего пользователя, и новый вишлист появляется в списке без перезагрузки (через обновление состояния на клиенте).

Страница конкретного вишлиста живёт по адресу `/wishlist/[id]`. На неё можно попасть либо как владелец через дашборд, либо как гость по публичной ссылке без регистрации. При загрузке страницы я достаю вишлист по id и определяю, владелец это или гость, сравнивая `wishlist.owner_id` с id текущего пользователя (если он залогинен).

В верхней части страницы показываю карточку с названием, датой (если есть) и небольшим бейджем. Если это владелец, бейдж говорит что‑то вроде «Это ваш вишлист», плюс текст‑подсказка, что прогресс сборов и резервы спрятаны, чтобы не расслаблять сюрприз. Если это гость, бейдж можно формулировать как «Вы гость, помогите выбрать подарок».

Ниже идёт список подарков из таблицы `items` для этого вишлиста. По каждому подарку отображаются название, цена (если она указана), ссылка, по которой можно открыть товар в новой вкладке, и мини‑картинка, если есть `image_url`. Для владельца здесь же будет доступна кнопка «Добавить подарок». Она открывает модалку, где можно ввести название, ссылку, цену и URL картинки. Дополнительно делаю поле «Вставить ссылку на товар» и кнопку «Автозаполнить»: при нажатии отправляю запрос на API route Next.js, который по этому URL попробует вытащить og‑теги (title, image, price) и подставит их в форму. Всё это можно отредактировать перед сохранением.

Резервы я реализую так. Для гостя возле подарка есть кнопка «Зарезервировать» (или «Снять резерв», если он уже забрал этот подарок). Под капотом это просто insert или delete в таблицу `reservations` с именем из поля `reserver_name`. Для владельца детали резервов не показываю, максимум — нейтральный текст «Этот подарок уже кто‑то взял» без имён.

Скидывания организую через `contributions`. Для подарков, у которых есть цена, сверху карточки будет прогресс‑бар. Процент считаю как сумму всех `contributions.amount` по этому подарку, делённую на `price`. Обновление прогресса делаю с анимацией. Рядом с прогресс‑баром будет кнопка «Скинуться», которая открывает модалку с полями «Сумма» и «Имя» (для гостя без логина). После успешного вклада прогресс‑бар и список вкладов обновляются. Владелец при этом видит только общую сумму собранного, например «7 000 из 10 000», без имён и индивидуальных сумм. Гости могут видеть список вкладов с именами и суммами, чтобы понимать, как продвигается сбор.

## Реалтайм
Хочу, чтобы список «жил» при просмотре несколькими людьми одновременно. Для этого буду использовать Supabase Realtime. Я подписываюсь на `postgres_changes` по таблицам `reservations` и `contributions` (с фильтрацией по item_id, который относится к текущему wishlist). При каждом insert или delete на клиенте, который открыт на этом вишлисте, обновляю локальное состояние подарков: меняется статус резерва и пересчитывается прогресс‑бар по сбору средств. Всё это должно происходить без перезагрузки страницы.

## Продуктовые решения и edge‑кейсы

Гость без регистрации может по ссылке открыть вишлист, зарезервировать подарок и скинуться. При первом действии я прошу ввести имя и сохраняю его в `localStorage`, чтобы дальше подставлять автоматически. На бэкенде это имя идёт в `reserver_name` и `contributor_name`.

Минимальный вклад я ставлю на уровне 100 рублей (или условного 1 у.е.), чтобы не было совсем символических сумм. Если пользователь вводит меньше, валидация формы подсказывает, что сумма слишком маленькая.

Если к определённой дате вся сумма по подарку не была собрана, я всё равно считаю его «частично профинансированным». Владелец видит, сколько собрано из плановой суммы и может либо доплатить сам, либо использовать эту информацию как ориентир. В интерфейсе это можно подсветить бейджем «Почти собрано».

Когда владелец удаляет подарок, по которому уже есть вклады, по схеме `on delete cascade` удаляются и связанные резервы и вклады. Перед этим в интерфейсе обязательно показываю предупреждение, что по этому подарку уже есть активности, и они тоже будут удалены. С учётом того, что в рамках теста это виртуальные суммы, этого достаточно.

Для резервов хочу запретить дублирование. На один подарок допускается только один активный резерв. Если кто‑то пытается забрать уже зарезервированный подарок, показываю сообщение «Этот подарок уже кто‑то взял, выберите другой». Это можно подкрепить UNIQUE‑ограничением на `item_id` в `reservations` или проверкой в коде.

Отменять резерв может только тот, кто его поставил (до даты события). Владелец и другие гости отменять чужие резервы не могут.

## Дизайн, анимации и UX
По стилю хочется современный, «праздничный» интерфейс без ощущения корпоративной админки. Это про подарки, внимание и заботу, и интерфейс должен это поддерживать.

Фон приложения делаю с мягким градиентом — например, от розового к голубому или фиолетово‑синему. Можно добавить лёгкий шум, чтобы не было ощущения плоской заливки. Карточки списков и подарков — в стиле glassmorphism: полупрозрачный белый фон, `backdrop-blur`, тонкая светлая рамка, мягкая тень и большие скругления. Кнопки — яркие градиентные (например, фиолетовый в розовый) с белым текстом и читабельными состояниями наведения и нажатия.

По типографике — крупные заголовки (в духе `text-3xl`–`text-4xl`), более лёгкие подзаголовки и опрятные подписи к полям. На мобильном заголовки можно центрировать, на десктопе выровнять по левому краю.

Empty‑состояния тоже хочу сделать живыми. Для пустого дашборда это может быть блок с emoji 🎁 и текстом «Здесь пока пусто. Создайте первый вишлист и отправьте его друзьям» плюс большая кнопка «Создать вишлист». Для пустого вишлиста — текст «В этом вишлисте пока нет подарков» и подсказка «Начните с того, что давно хотели, но не покупали себе сами».

Анимации — аккуратные, без перегруза. Для карточек при наведении даю лёгкий подъём и усиление тени через transform и transition. Открытие модалок делаю с умеренным scale + opacity. Кнопки ведут себя «по‑кнопочному» — небольшое сжатие при нажатии. Прогресс‑бар для сборов заполняется плавно, без рывков. И отдельная фишка — конфетти: когда по подарку сбор закрыт (100%), на пару секунд запускаю анимацию конфетти поверх экрана и затем её убираю.
